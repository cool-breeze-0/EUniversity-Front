<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.euniversity">
    <!-- 轮播器：if you want to load images from the internet -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.home.HomeUniversityDetailsActivity"></activity>
        <activity android:name=".ui.community.CommunityAnswerDetailsActivity" />
        <activity android:name=".ui.community.CommunityAskOrAnswerActivity" />
        <activity android:name=".ui.community.CommunityProblemDetailsActivity" />
        <activity android:name=".ui.community.CommunityGroupActivity" />
        <activity android:name=".ui.predict.PredictResultActivity" />
        <activity android:name=".ui.user.UserApplicationContentActivity" />
        <activity android:name=".ui.user.UserAccountActivity" />
        <activity android:name=".ui.user.UserProfileInformationActivity" />
        <activity
            android:windowSoftInputMode="stateAlwaysHidden|adjustPan"
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>